<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <!-- Allow cleartext (HTTP) traffic -->
        <trust-anchors>
            <!-- Add any custom trust anchors here if needed -->
        </trust-anchors>
    </base-config>
</network-security-config>
